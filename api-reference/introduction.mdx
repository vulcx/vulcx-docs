---
title: "Vulcx API reference — quote, swap & instructions"
sidebarTitle: "Introduction"
description: "Get started with the Vulcx API for SVM chains: base URL, the {success, data, error} response envelope, status codes, authentication, and the full endpoint reference."
llmDescription: "Vulcx API reference overview. Base URL: https://api.vulcx.xyz. Vulcx is an SVM-chain aggregator: live on Fogo today, Solana support in progress. Covers the JSON response envelope ({success, data, error}), HTTP status codes (200, 400, 401, 403, 404, 429, 500), the auth model (an API key is optional on REST endpoints — GET /api/v1/quote, POST /api/v1/swap, POST /api/v1/instructions, GET /api/v1/price — anonymous requests are allowed but capped at 2 cost units per second, burst 20; a key raises that to the account's plan budget, from 20 cost units per second, burst 40, on Free up to 2000, burst 4000, on Pro; WS /api/v1/stream still requires a key; see /get-started/authentication), and the endpoint table with an auth-required column linking to GET /api/v1/quote, POST /api/v1/swap, POST /api/v1/instructions, WS /api/v1/stream, GET /api/v1/price, and GET /health."
---

**Base URL**: `https://api.vulcx.xyz`

The API runs in Amsterdam, Netherlands, with no CDN in front of it. For the lowest latency, host your server nearby and reuse connections: see [Server location & latency](/get-started/latency).

All swap endpoints are under `/api/v1`. The API currently operates on **Fogo**.

<Note>
An API key (`Authorization: Bearer vulcx_...`) is **optional** on REST endpoints — you can call
`/quote`, `/swap`, `/instructions`, and `/price` with no key at all. Anonymous requests are capped
at a small per-IP rate limit; add a free key for the full published budget. The
**WebSocket stream** (`GET /api/v1/stream`) is the one exception — it always requires a key
(`?key=`). See [Authentication](/get-started/authentication) and [Rate limits](/api-reference/rate-limits).
</Note>

## Response format

All responses use a consistent envelope:

**Success:**
```json
{
  "success": true,
  "data": { ... }
}
```

**Error:**
```json
{
  "success": false,
  "error": "error message describing what went wrong"
}
```

## HTTP status codes

| Code | Meaning |
|------|---------|
| `200` | Success |
| `400` | Bad request: invalid parameters, missing required fields |
| `401` | No API key was provided. Only `GET /api/v1/stream` returns this; REST allows anonymous callers |
| `403` | The API key is invalid, disabled, or revoked |
| `404` | Not found: no route exists for the given token pair/amount |
| `429` | Rate limited: too many requests |
| `500` | Internal error building the transaction. A failed *simulation* is not a 500 — it is a `400`, `409` or `422` |

## Endpoints

| Endpoint | Method | Auth required | Description |
|----------|--------|:---:|-------------|
| `GET https://api.vulcx.xyz/api/v1/quote` | GET | Optional | Get swap quote with routing details |
| `POST https://api.vulcx.xyz/api/v1/swap` | POST | Optional | Build unsigned swap transaction |
| `POST https://api.vulcx.xyz/api/v1/instructions` | POST | Optional | Get raw swap instructions |
| `WS wss://api.vulcx.xyz/api/v1/stream` | WS | ✅ | [Stream live quotes over WebSocket](/api-reference/stream/index) |
| `POST https://api.vulcx.xyz/api/v1/cpi/route-accounts` | POST | Optional | [Route instruction for a program to CPI into](/api-reference/cpi-route-accounts) |
| `GET https://api.vulcx.xyz/api/v1/price/:mint` | GET | Optional | [Spot USD price for a token](/api-reference/price/index) |
| `GET https://api.vulcx.xyz/api/v1/price?mints=` | GET | Optional | Spot USD prices for several tokens in one call |
| `GET https://api.vulcx.xyz/api/v1/tokens` | GET | — | The token list the router knows, with decimals and logos |
| `GET https://api.vulcx.xyz/health` | GET | — | Service health check |
| `GET https://api.vulcx.xyz/health/swap` | GET | — | Swap canary: whether a real quote still routes |

<Note>
"Optional" means the endpoint works with no key, but is then subject to a small anonymous-tier
rate limit rather than your plan's rate limit. See [Rate limits](/api-reference/rate-limits).
</Note>
