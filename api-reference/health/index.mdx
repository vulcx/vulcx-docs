---
title: "Health Check"
description: "GET /health returns the Vulcx API status with no API key required — poll it to monitor uptime and alert on non-200 responses in production."
llmDescription: "OpenAPI reference for GET /health on the Vulcx API. Lightweight service health check returning service status. No authentication required. Use for uptime monitoring."
openapi: "GET /health"
---
